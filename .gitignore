node_modules/
dist/
.nuxt/
.output/
*.sock
.DS_Store

# Playwright
test-results/
playwright-report/

# Stunt node (developer-specific)
test/stunt-node/config.local

# Claude Code local state
.claude/

# Release tarballs (packaging/make-release.sh output)
dist-release/

# Credential material — never commit secrets, even test ones (belt-and-suspenders;
# real secrets belong in root-only files on the node, never in the repo).
*.pw
*.cred
*.secret
**/creds/
